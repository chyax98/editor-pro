/**
 * File-Ops 模块 - 文件操作
 *
 * 包含：
 * - yaml-manager: YAML 自动更新
 * - tag-manager: 标签管理
 * - tag-modal: 标签重命名弹窗
 * - save-cleaner: 保存时清理
 * - inline-decorator: 行内装饰（图标、头图）
 */

// YAML 管理
export { YamlManager } from "./yaml-manager";

// 标签管理
export { TagManager } from "./tag-manager";
export { TagRenameModal } from "./tag-modal";

// 保存清理
export { SaveCleaner } from "./save-cleaner";

// 行内装饰
export { InlineDecorator } from "./inline-decorator";
